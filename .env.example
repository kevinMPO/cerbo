# =============================================================================
# CERBO — env template. Names only, no values. Copy to .env.local and fill in.
# =============================================================================

# ---- LLM: OpenAI (primary) ----
OPENAI_API_KEY=
OPENAI_MODEL=

# ---- LLM: OpenRouter (fallback) ----
OPENROUTER_API_KEY=
OPENROUTER_MODEL=

# ---- Linkup (MCP / firmographic enrichment) ----
LINKUP_API_KEY=

# ---- ElevenLabs (audio memo TTS) ----
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# ---- Convex (populated by `npx convex dev`) ----
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
